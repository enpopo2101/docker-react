sudo: required
services: 
  - docker

before_install: 
  - sudo gem install bundler
  - docker build -t enpopo2101/docker-react -f Dockerfile.dev .

script:
  - docker run enpopo2011/docker-react npm run test -- --coverage